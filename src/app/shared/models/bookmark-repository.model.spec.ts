import { BookmarkRepository } from './bookmark-repository.model';

describe('BookmarkRepository', () => {
  it('should create an instance', () => {
    expect(new BookmarkRepository()).toBeTruthy();
  });
});
